<html>

<head>
  <style>
    body {
      font-family: Arial, Helvetica, sans-serif;
    }
  </style>
</head>

<body>
  <strong>Hello, {{ ad.author.name }}</strong>
  <p>You hava a new purchase request for your "{{ ad.title }}" ad.</p>
  <br>
  <p>Customer info:</p>
  <br>
  <strong>{{ user.name }} ({{ user.email }}):</strong>
  <p>{{ content }}</p>
</body>

</html>
